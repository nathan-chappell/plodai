Drop local development TLS files here if you generate them outside Windows.

Expected names:
- dev-cert.pem
- dev-key.pem

I could not find `openssl` or `mkcert` on this Windows environment, so generating them from WSL is the easiest next step.
